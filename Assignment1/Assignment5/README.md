# Sandwich Maker API - CRUD Operations

## Overview
This project implements CRUD operations using FastAPI and MySQL for a Sandwich Maker Machine system.

## Run Instructions
pip install -r requirements.txt
uvicorn api.main:app --reload